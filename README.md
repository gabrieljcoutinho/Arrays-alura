# Laboratório de Manipulação de Arrays em JavaScript

Este repositório contém uma série de scripts e exercícios práticos focados no domínio de métodos de arrays e estruturas de repetição em JavaScript. O objetivo é servir como um guia de referência para operações comuns de lógica de programação.

---

## 📂 Organização do Projeto

Os arquivos estão divididos por conceitos fundamentais, cobrindo desde operações básicas até manipulações complexas de matrizes.

### 1. Operações Básicas e Atualização
* **Adicionar/Remover**: Scripts para `push`, `pop`, `shift` e `unshift`.
* **Atualizar Lista**: Uso de `splice` para substituir elementos em posições específicas.
* **Dividir e Juntar**: Utilização de `slice` para segmentar arrays e `concat` para unir listas.

### 2. Iteração e Cálculos
* **Médias com For**: Cálculo de média aritmética simples utilizando o laço `for` clássico.
* **Médias com ForEach**: Implementação de lógica de soma e divisão utilizando métodos modernos de iteração.
* **Soma de Médias**: Acumuladores de valores dentro de estruturas de repetição.

### 3. Filtros e Buscas
* **Procurar na Lista**: Implementação de buscas para localizar itens específicos.
* **Filtrar Dados**: Uso do método `filter` para criar subconjuntos (ex: notas acima da média).
* **Remover Duplicados**: Lógica para limpeza de dados e garantia de elementos únicos.

### 4. Transformação de Dados
* **Adicionar Pontos**: Uso de `map` para transformar valores (ex: aplicar bônus em notas).
* **Criar Cópias**: Técnicas para clonar arrays sem referenciar o original (Spread Operator).
* **Matrizes (Arrays de Arrays)**: Exercícios de acesso e manipulação de listas multidimensionais.

---

## 🚀 Como Utilizar

### Requisitos Especiais de Instalação
Para executar estes scripts, você precisará ter o **Node.js** instalado em sua máquina.

1. **Clone o repositório:**
   ```bash
   git clone [https://github.com/seu-usuario/nome-do-repositorio.git](https://github.com/seu-usuario/nome-do-repositorio.git)
